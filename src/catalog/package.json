{
  "name": "catalog_api",
  "version": "1.0.0",
  "description": "Catalog API for GimmeProteinWebApp",
  "main": "index.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf ../build/catalog && tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.20",
    "typescript": "^4.9.4"
  }
}
